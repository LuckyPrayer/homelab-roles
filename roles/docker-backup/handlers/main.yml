---
# Docker Backup Role - Handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: true

- name: Restart backup timer
  ansible.builtin.systemd:
    name: docker-backup.timer
    state: restarted
  become: true
  when: backup_enabled | bool
